MEMORY {
  MAIN:		start = $1bff, size = $3001;
  ZP:		start = $fb, size = 5;
}

SEGMENTS {
  BHDR:		load = MAIN, define = yes;
  CODE:		load = MAIN;
  DATA:		load = MAIN;
  BSS:		load = MAIN, type = bss;
  ALBSS:	load = MAIN, type = bss, align = $100;
  ZEROPAGE:	load = ZP, type = zp;
}
