MEMORY {
  MAIN:		start = $0fff, size = $3001;
}

SEGMENTS {
  BHDR:		load = MAIN;
  CODE:		load = MAIN;
  DATA:		load = MAIN;
  BSS:		load = MAIN, type = bss;
  ALBSS:	load = MAIN, type = bss, align = $100;
}

