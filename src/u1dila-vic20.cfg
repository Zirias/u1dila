MEMORY {
  MAIN:		start = $0fff, size = $e01;
  ZP:		start = $fb, size = 5;
}

SEGMENTS {
  BHDR:		load = MAIN, define = yes;
  CODE:		load = MAIN;
  DATA:		load = MAIN;
  BSS:		load = MAIN, type = bss;
  ZEROPAGE:	load = ZP, type = zp;
}
